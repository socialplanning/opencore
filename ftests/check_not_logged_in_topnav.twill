#
# makes sure topnav is correct when not logged in
#
# expects: 
#    not logged in
#

go ${base_url}
find 'oc-topnav-context.*People.*${projtxt}s.*Start A ${projtxt}.*oc-usermenu-explore.*topnav_explore_home.*topnav_explore_people.*topnav_explore_projects.*topnav_explore_new_project.*oc-topnav-usermenu.*Sign in.*Create account' si
