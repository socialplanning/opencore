#
# disable default redirect by deleting the utility
#
# $admin - zope administrative user
# $adminpw - zope admin password 
#

extend_with flunc.zope_cleanup

zope_delobject 'utilities' 'IDefaultRedirectInfo' $admin $adminpw