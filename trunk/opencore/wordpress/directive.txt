=======================
 wordpress directive
=======================

    >>> from opencore.wordpress.interfaces import IWordPressInfo
    >>> getUtility(IWordPressInfo)
    <opencore.wordpress.uri.WordpressURI object at ...>

    >>> wp_info = getUtility(IWordPressInfo)
    >>> wp_info.uri
    u'http://nohost:wordpress'

    >>> wp_info.external_uri
    u'blog'
