<configure
   xmlns="http://namespaces.zope.org/zope">
  <!-- @@ really we should use site.zcml or in package-includes to
       call opencore.configuration rather than depending on Five's
       magic for loading configure and meta zcml.

       But for now, we daisy chain 
    -->
  <include package="opencore.configuration" file="meta.zcml" />
</configure>
